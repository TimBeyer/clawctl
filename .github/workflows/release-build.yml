name: Release Build

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run build:release
      - run: cd dist && zip clawctl-darwin-arm64.zip clawctl
      - name: Upload binary and publish release
        run: |
          gh release upload "${{ github.event.release.tag_name }}" dist/clawctl-darwin-arm64.zip --clobber
          gh release edit "${{ github.event.release.tag_name }}" --draft=false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
