export {
  loadRegistry,
  saveRegistry,
  addInstance,
  removeInstance,
  getInstance,
  listInstances,
} from "@clawctl/host-core";
export type { RegistryEntry, Registry } from "@clawctl/host-core";
