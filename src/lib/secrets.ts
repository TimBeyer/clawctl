export {
  resolveOpRefs,
  findSecretRefs,
  hasOpRefs,
  resolveEnvRefs,
  getNestedValue,
  setNestedValue,
} from "@clawctl/host-core";
export type { SecretRef, ResolvedSecretRef } from "@clawctl/host-core";
